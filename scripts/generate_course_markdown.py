from __future__ import annotations

from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]


def write(path: Path, content: str) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(content.strip() + "\n", encoding="utf-8")


def main() -> None:
    files: dict[Path, str] = {
        ROOT / "docs" / "8086" / "md" / "3.1.md": """
# 8086 寻址方式和机器码摘要

## 课件定位

- 对应课件：`微机原理-第3章1寻址方式和机器码.pptx`
- 主题：8086 指令格式、操作数类型、寻址方式、机器码基础

## 本讲主线

这份课件先从“什么是汇编语句”入手，再过渡到“CPU 如何找到操作数”。真正的核心是把各种寻址方式和默认段寄存器理清楚。

## 页序摘要

### 1. 课程导入

- 介绍第 3 章主题：8086 的寻址方式和指令系统。
- 用 `MOV` 例子说明一条指令由操作码和操作数构成。

### 2. 汇编语句与指令格式

- 区分执行性语句和说明性语句。
- 说明操作码、目的操作数、源操作数、注释的书写格式。
- 引出 `i8/i16/r8/r16/m8/m16` 等操作数记号。

### 3. 立即数与寄存器寻址

- 立即数直接写在指令中，只能作源操作数。
- 寄存器寻址速度快，操作数直接来自寄存器。
- 强调 8 位和 16 位寄存器宽度要匹配。

### 4. 存储器寻址方式

- 直接寻址：偏移地址在指令中给出。
- 寄存器间接寻址：偏移地址放在 `BX/BP/SI/DI` 中。
- 相对寻址：寄存器加位移量。
- 基址变址寻址：基址寄存器与变址寄存器组合。
- 相对基址变址寻址：再叠加位移量。

### 5. 默认段寄存器和物理地址

- 默认情况下 `BX/SI/DI` 走 `DS`。
- 只要用了 `BP`，默认段一般变成 `SS`。
- 物理地址按 `段地址 × 10H + 偏移地址` 计算。
- 课件通过多组已知寄存器值练习物理地址求法。

### 6. 机器码基础

- 说明机器指令在代码段中的组织方式。
- 介绍操作码和寻址方式如何体现在机器码中。
- 强调汇编写法和机器内部编码是一一对应关系。

## 应掌握的结论

- 看到内存操作数先判断寻址方式，再判断默认段寄存器。
- 看到 `BP` 默认想到 `SS`。
- 看到 `OFFSET` 取地址，普通变量名多半取内容。
- 立即数不能作双操作数指令的目的操作数。

## 复习重点

- 五类常见寻址方式的写法。
- 每类寻址方式的物理地址计算。
- 执行性语句与说明性语句的区别。
""",
        ROOT / "docs" / "8086" / "md" / "3.2.md": """
# 8086 数据传送指令摘要

## 课件定位

- 对应课件：`微机原理-第3章2指令之数据传送.pptx`
- 主题：数据传送类指令、寻址应用、堆栈、I/O 端口、地址传送

## 本讲主线

这部分围绕“数据从哪里来到哪里去”展开。课件先复习寻址方式，然后系统整理 `MOV`、`PUSH/POP`、`XCHG`、`IN/OUT`、`LEA/LDS/LES` 等指令。

## 页序摘要

### 1. 寻址方式复习

- 通过 `MOV AX,[SI+06H]`、`MOV AX,[BX+SI]` 等例子复习源操作数寻址。
- 配合已知 `DS/ES/SS/BX/BP/SI` 数值计算物理地址。

### 2. 数据传送类指令总览

- 常见来源和去向包括寄存器、存储器、立即数、段寄存器、端口、标志寄存器。
- 大多数数据传送类指令不影响标志位。

### 3. MOV

- 给出合法格式：寄存器、内存、立即数之间的传送。
- 强调位宽匹配、`BYTE PTR/WORD PTR` 的必要性。
- 强调不能内存到内存。
- 强调段寄存器不能直接接立即数，`CS` 不能作为 `MOV` 目的操作数。

### 4. 数据定义和地址

- 结合 `DB`、`DW`、`DUP` 说明变量布局。
- 用 `OFFSET` 区分“取内容”和“取偏移地址”。
- 引出小端存储规则。

### 5. PUSH / POP

- 说明栈在 `SS` 段中，`SP` 指向栈顶。
- 栈按字操作，方向为高地址到低地址。
- 说明入栈、出栈过程中 `SP` 的变化。
- 举例说明后进先出。

### 6. XCHG 和 XLAT

- `XCHG` 用于寄存器和寄存器/内存交换。
- `XLAT` 用于查表，隐含使用 `BX` 和 `AL`。

### 7. IN / OUT

- 区分直接端口地址和 `DX` 间接端口地址。
- 强调数据寄存器固定为 `AL/AX`。
- 强调端口号超过 `FFH` 必须走 `DX`。

### 8. LEA / LDS / LES

- `LEA` 取有效地址，不取内容。
- `LDS`、`LES` 从内存装入远指针。
- 对比 `LEA` 和 `MOV` 的差别。

### 9. 标志寄存器传送

- `LAHF/SAHF` 处理标志低字节。
- `PUSHF/POPF` 保存和恢复整个标志寄存器。

## 应掌握的结论

- `MOV` 最容易错在宽度、段寄存器、内存到内存。
- `PUSH/POP` 只操作字。
- `IN/OUT` 只允许 `AL/AX` 传数据。
- `LEA` 取地址，`MOV` 取内容。
""",
        ROOT / "docs" / "8086" / "md" / "3.3.md": """
# 8086 算术、逻辑与移位指令摘要

## 课件定位

- 对应课件：`微机原理-第3章3指令之算数、逻辑、移位指令.pptx`
- 主题：算术运算、乘除法、BCD 调整、逻辑运算、移位与循环移位

## 本讲主线

这一讲把算数类和按位类指令集中梳理，重点不只是公式，而是“哪些标志位会变”和“什么场景该用哪类指令”。

## 页序摘要

### 1. 基本格式回顾

- 再次区分执行性语句与说明性语句。
- 回顾双操作数和单操作数指令的通用规则。

### 2. 加减类指令

- `ADD`、`ADC`、`INC`。
- `SUB`、`SBB`、`DEC`、`NEG`。
- 说明 `ADC/SBB` 在多字节运算中的作用。
- 强调 `INC/DEC` 不影响 `CF`。

### 3. 比较与标志位

- `CMP` 只改标志位，不保存结果。
- 无符号比较主要看 `CF` 和 `ZF`。
- 有符号比较要结合 `SF` 与 `OF`。

### 4. 乘除法

- `MUL/IMUL` 的隐含寄存器规则。
- `DIV/IDIV` 的被除数位置和商余数位置。
- `CBW/CWD` 用于带符号扩展。
- 强调商溢出会触发除法错误。

### 5. BCD / ASCII 调整

- `DAA/DAS` 用于压缩 BCD 加减法后调整。
- `AAA/AAS` 用于非压缩 BCD 或 ASCII 加减法后调整。
- `AAM/AAD` 分别服务乘法后、除法前处理。

### 6. 逻辑运算

- `NOT`、`AND`、`OR`、`XOR`、`TEST`。
- 说明清零、置位、翻转、掩码测试等典型用途。
- 强调 `TEST` 和 `CMP` 类似，只改标志位。

### 7. 移位和循环移位

- `SHL/SAL`、`SHR`、`SAR`。
- `ROL/ROR`、`RCL/RCR`。
- 说明逻辑右移和算术右移的区别。
- 说明 `RCL/RCR` 会把 `CF` 一起纳入循环链。

## 应掌握的结论

- 多精度加减一定想到 `ADC/SBB`。
- 带符号除法前常要 `CBW/CWD`。
- `SHR` 适合无符号数，`SAR` 适合有符号数。
- 逻辑题大多都能转成掩码与位操作。
""",
        ROOT / "docs" / "8086" / "md" / "3.4.md": """
# 8086 字符串与控制转移指令摘要

## 课件定位

- 对应课件：`微机原理-第3章4指令之字符串、处理控制转移.pptx`
- 主题：条件转移、调用返回、中断、字符串处理指令与重复前缀

## 本讲主线

这一讲把“程序流程如何改变”和“连续数据如何批量处理”放在一起。前半段是跳转/调用/中断，后半段是字符串指令族。

## 页序摘要

### 1. 控制转移基础

- 介绍近转移、段内/段间转移以及目的地址的不同寻址形式。
- 强调控制转移会改变 `IP`，远转移还会改变 `CS`。

### 2. 条件转移

- 解释 `JZ/JE`、`JNZ/JNE`、`JS`、`JC/JB`、`JA/JAE/JBE` 等含义。
- 说明这些跳转本质上依赖前一条运算或比较指令设置的标志位。
- 区分有符号比较和无符号比较。

### 3. CALL / RET / INT / IRET

- `CALL` 保存返回地址后进入子程序。
- `RET` 从栈中取回返回地址。
- `INT i8` 进入中断服务程序。
- `IRET` 用于中断返回。

### 4. 字符串指令的隐含约定

- 源串通常在 `DS:SI`。
- 目的串通常在 `ES:DI`。
- `CX` 常作为长度计数器。
- `DF` 决定地址自增还是自减。

### 5. 重复前缀

- `REP`：重复执行直到 `CX=0`。
- `REPE/REPZ`：边重复边要求 `ZF=1`。
- `REPNE/REPNZ`：边重复边要求 `ZF=0`。

### 6. 字符串传送与比较

- `MOVSB/MOVSW`：串传送。
- `CMPSB/CMPSW`：串比较。
- 课件用 `REP MOVS`、`REPE CMPSB` 展示批量复制和密码串比较。

### 7. 扫描、装入、存入

- `SCASB/SCASW`：在串中扫描目标值。
- `LODSB/LODSW`：把源串元素装入 `AL/AX`。
- `STOSB/STOSW`：把 `AL/AX` 写入目标串。

## 应掌握的结论

- 条件转移前一定要先看是谁设置了标志位。
- 字符串题先初始化 `SI/DI/CX/DF` 再写主体。
- `REP/REPE/REPNE` 的退出条件要和 `ZF` 一起判断。
""",
        ROOT / "docs" / "8086" / "md" / "4.0.md": """
# 汇编程序设计摘要

## 课件定位

- 对应课件：`微机原理-第4章汇编程序设计.pptx`
- 主题：源程序格式、伪指令、DOS/BIOS 调用、顺序/分支/循环/子程序设计

## 本讲主线

第 4 章把前面学过的指令组织成“可运行的汇编程序”。课件结构很长，但可以压缩成三个部分：程序框架、系统调用、设计方法。

## 页序摘要

### 1. 课程结构和重点

- 课件开头给出三块内容：程序格式与伪指令、DOS/BIOS 调用、程序设计方法。
- 教学重点落在源程序框架、变量/标号、基本程序结构和子程序设计。

### 2. 汇编源程序格式

- 区分硬指令和伪指令。
- 说明名字、标号、操作码、操作数、注释的标准写法。
- 用 `END`、`SEGMENT`、`ENDS`、`ASSUME`、`PROC`、`ENDP` 等伪指令组织程序。

### 3. 常量、变量和操作符

- 介绍 `DB/DW/DD`、字符/字符串定义、重复定义。
- 介绍 `OFFSET`、`SEG`、`PTR`、`TYPE`、`LENGTH` 等操作符。
- 强调变量类型和存储空间大小要和指令匹配。

### 4. 完整程序框架

- 展示含代码段、数据段、堆栈段、附加段的完整模板。
- 说明何时必须设置 `ES`，尤其是字符串处理程序。
- 强调 `DS/SS/ES` 的初始化。

### 5. DOS / BIOS 系统调用

- 用 `INT 21H` 完成字符串输出、键盘输入、程序返回等任务。
- 用 `INT 10H`、`INT 16H` 完成显示和键盘底层服务。
- 课件通过缓冲区输入输出例子说明系统功能号和寄存器约定。

### 6. 顺序、分支、循环程序设计

- 用基本结构组织算法。
- 强调先画流程图，再决定寄存器分工。
- 例题覆盖分类统计、范围判断、查表和数制转换。

### 7. 子程序设计

- 说明参数如何通过寄存器或内存传递。
- 说明返回值如何通过寄存器返回。
- 强调现场保护与恢复。
- 给出十进制转二进制、字符显示等过程例子。

## 应掌握的结论

- 能写出完整汇编程序骨架。
- 能区分硬指令、伪指令、操作符。
- 能把题目拆成顺序、分支、循环、子程序四种结构。
- 需要与操作系统交互时优先想到 `INT 21H`。
""",
        ROOT / "docs" / "8086" / "md" / "5.1.md": """
# 存储器摘要

## 课件定位

- 对应课件：`微机原理-第5章1存储器.pptx`
- 主题：存储器分类、RAM/ROM/Flash、性能指标、芯片结构、扩展与片选

## 本讲主线

这份课件前半讲器件知识，后半讲接口设计。真正与 8086 实验和习题最紧的是：容量、位宽、地址线、片选和扩展。

## 页序摘要

### 1. 存储器分类

- 从内部存储器、外部存储器、Cache 切入。
- 区分 RAM、ROM、半导体存储器、磁存储器、光存储器。
- 再按易失性、非易失性、随机/顺序/直接存取方式细分。

### 2. RAM

- `SRAM`：速度高、无需刷新、容量较小。
- `DRAM`：容量大、成本低、需要刷新。
- 介绍常见刷新方式和内存条演进。

### 3. ROM 家族

- `PROM`、`EPROM`、`EEPROM`、`Flash` 的擦写方式和用途不同。
- 课件用 `2716/2764/2817A/2864A` 等芯片说明典型引脚和应用。
- 强调掉电保存参数、程序固化等使用场景。

### 4. 性能指标

- 容量。
- 存取时间。
- 存储周期。
- 带宽和成本。

### 5. 芯片结构与典型引脚

- 以 SRAM、EPROM 为例说明地址线、数据线和控制信号。
- 强调片内地址线数量由芯片容量决定。

### 6. 存储器扩展与片选

- 用位扩展解决位宽不足。
- 用字扩展适配 8086 的 16 位数据总线。
- 用容量扩展拼接更大的地址空间。
- 讲解线选法、全译码法、部分译码法三类片选思路。

## 应掌握的结论

- `SRAM` 和 `DRAM` 的区别一定要能快速说清。
- 芯片扩展题先算地址线、再看位宽、最后写片选。
- 8086 连接存储器时必须考虑高低字节体和 `A0/BHE#`。
""",
        ROOT / "docs" / "8086" / "md" / "6.1.md": """
# I/O 接口摘要

## 课件定位

- 对应课件：`微机原理-第6章1IO接口.pptx`
- 主题：接口作用、端口编址、IN/OUT、程序查询、中断、DMA

## 本讲主线

本讲先解释“为什么需要接口”，再讲“CPU 怎样和接口交换数据”。核心内容是端口编址方式和三种传送方式。

## 页序摘要

### 1. 接口存在的原因

- 外设与 CPU 在速度、电平、数据格式、时序上不匹配。
- 因此需要接口电路做缓冲、译码、锁存、控制和握手。

### 2. 端口和编址方式

- 介绍存储器映像编址。
- 介绍独立 I/O 编址。
- 对比两者在地址空间和指令使用上的区别。

### 3. IN / OUT 指令

- 复习端口访问格式。
- 区分直接端口地址和 `DX` 间接端口地址。
- 结合状态口、数据口、控制口举例。

### 4. 程序控制方式

- CPU 轮询外设状态，准备好再读写。
- 结构简单，但 CPU 会忙等。

### 5. 中断方式

- 设备就绪后向 CPU 发中断请求。
- CPU 在合适时机转去执行中断服务程序。
- 适合中速设备。

### 6. DMA 方式

- 数据直接在外设和内存之间传输。
- CPU 只做初始化和结束处理。
- 适合大批量高速传输。

## 应掌握的结论

- 接口题先想“译码、握手、状态、控制”四件事。
- 三种数据传送方式要会比较优缺点。
- 独立编址时一般通过 `IN/OUT` 访问端口。
""",
        ROOT / "docs" / "8086" / "md" / "6.2.md": """
# 并行接口 8255 摘要

## 课件定位

- 对应课件：`微机原理-第6章2并行8255.pptx`
- 主题：8255 端口结构、控制字、工作方式、键盘/显示/打印接口

## 本讲主线

这份课件把 8255 当成标准并行接口芯片来讲。核心问题是端口分组、控制字书写和模式 0/1/2 的使用场景。

## 页序摘要

### 1. 芯片结构

- 8255 提供 `PA/PB/PC` 三个 8 位端口。
- `PC` 可拆分成高 4 位和低 4 位。
- A 组和 B 组分别有独立控制逻辑。

### 2. 工作方式

- 方式 0：基本输入输出。
- 方式 1：选通输入输出，有握手。
- 方式 2：双向总线方式，仅 A 组支持。

### 3. 控制字

- 一类控制字用于设置端口方向和工作方式。
- 另一类 BSR 控制字用于单独置位/复位 `PC` 的某一位。
- 课件多次要求根据题意写控制字。

### 4. 典型应用

- 打印机并行接口。
- 数码管显示。
- 小键盘输入。
- 开关量采集与控制。

### 5. 实验型题目

- 根据端口地址读写数据。
- 根据输入/输出方向和方式写模式控制字。
- 根据握手需求判断哪些 `PC` 位被占用。

## 应掌握的结论

- 先判 A 组和 B 组，再写控制字。
- 方式 2 只属于 A 组。
- `PC` 在方式 1/2 下不一定能全部当普通 I/O 位使用。
""",
        ROOT / "docs" / "8086" / "md" / "7.0.md": """
# 8253 定时计数器摘要

## 课件定位

- 对应课件：`微机原理-第7章8253.pptx`
- 主题：8253/8254 结构、控制字、六种工作方式、定时/分频应用

## 本讲主线

课件把 8253 作为典型可编程定时器来讲。需要掌握的不是所有波形细节，而是控制字含义、六种模式特点和初值计算。

## 页序摘要

### 1. 芯片和引脚

- 8253 有 3 个独立计数通道。
- 每个通道有 `CLK`、`GATE`、`OUT`。
- 另有控制字寄存器统一配置工作模式。

### 2. 控制字

- 指定通道号。
- 指定读写格式。
- 指定工作方式。
- 指定二进制或 BCD 计数。

### 3. 六种工作方式

- 方式 0：计数结束输出。
- 方式 1：可编程单稳。
- 方式 2：分频器。
- 方式 3：方波发生器。
- 方式 4：软件触发选通。
- 方式 5：硬件触发选通。

### 4. 编程与读写

- 先向控制口写控制字。
- 再按规定格式写入计数初值。
- 必要时读取当前计数值或等待 `OUT` 变化。

### 5. 应用举例

- 周期定时。
- 方波产生。
- 事件计数。
- 多路并行高斯或分频题的接口实现。

## 应掌握的结论

- 做题先选模式，再算初值。
- 方式 2 和方式 3 都能分频，但输出波形不同。
- 读写顺序必须与控制字中设定的格式一致。
""",
        ROOT / "docs" / "信息论与编码" / "md" / "01-introduction.md": """
# 信息论与编码 第 1 章摘要

## 课件定位

- 对应课件：`第1章 绪论.pdf`
- 主题：课程目标、研究对象、信息论与编码的基本问题

## 本讲主线

绪论主要搭建全课程视角：信息论负责建立数学极限，编码理论负责构造实现方法。

## 页序摘要

### 1. 课程说明

- 介绍教材、参考书、考核要求和资料渠道。
- 说明本课程是电子通信类的重要专业基础课。

### 2. 信息论与编码的研究范围

- 狭义信息论：以香农理论为核心，研究信息量度和高效、可靠、安全传输。
- 广义信息论：扩展到识别、翻译、控制等更广的领域。

### 3. 通信系统模型

- 信源、编码器、信道、解码器、信宿。
- 课程的后续各章都围绕这个模型展开。

### 4. 本课程三条主线

- 信息量如何度量。
- 信源如何压缩。
- 信道中如何可靠传输。

## 应掌握的结论

- 熵、互信息、容量是全课最核心的三个量。
- 信源编码和信道编码分别对应压缩与抗差错两个方向。
""",
        ROOT / "docs" / "信息论与编码" / "md" / "02-source-and-entropy.md": """
# 信息论与编码 第 2 章摘要

## 课件定位

- 对应课件：`第2章 信源及信源熵.pdf`
- 主题：信源建模、自信息、熵、互信息、熵率、冗余度

## 本讲主线

这一章把“消息”抽象成随机变量或随机过程，再用概率定义平均信息量。

## 页序摘要

### 1. 信源的描述和分类

- 区分离散/连续、单符号/序列、无记忆/有记忆信源。
- 强调用概率论语言描述信源。

### 2. 单符号离散信源

- 定义自信息 `I(x)=-log p(x)`。
- 定义熵 `H(X)` 作为平均不确定度。
- 说明等概率分布时熵最大。

### 3. 条件熵与互信息

- 已知一个变量后剩余不确定度用条件熵表示。
- 两个变量共享的信息量用互信息表示。

### 4. 离散序列信源的熵

- 引出熵率。
- 强调相关性会降低单位符号平均信息量。

### 5. 连续信源的熵和互信息

- 用概率密度和微分熵描述连续情形。
- 与离散熵做性质区分。

### 6. 冗余度

- 用来衡量信源中可压缩的冗余部分。

## 应掌握的结论

- 熵是无失真压缩极限的基础。
- 互信息既在本章出现，也会在信道容量和率失真中继续出现。
""",
        ROOT / "docs" / "信息论与编码" / "md" / "03-channel-and-capacity.md": """
# 信息论与编码 第 3 章摘要

## 课件定位

- 对应课件：`第3章 信道与信道容量.pdf`
- 主题：离散信道、BSC、强对称/准对称信道、DMC、连续信道、香农公式

## 本讲主线

第 3 章的中心任务是定义容量，并学会在不同信道模型下求容量。

## 页序摘要

### 1. 信道模型与容量概念

- 给出编码信道和狭义信道模型。
- 容量定义为互信息最大值。

### 2. 二进制对称信道 BSC

- 通过信道矩阵描述转移概率。
- 推出容量与误码率的关系。

### 3. 单符号离散信道容量

- 讨论强对称信道和准对称信道。
- 说明何时输入等概率可达到容量。

### 4. 离散无记忆序列信道

- 从单符号容量推广到序列容量。
- 利用无记忆性分解分析。

### 5. 连续信道及其容量

- 分析高斯信道容量。
- 引入带宽、功率、信噪比对容量的影响。
- 给出香农公式。

## 应掌握的结论

- `C=max I(X;Y)` 是全章核心。
- BSC、强对称信道、高斯信道是最常用的三种模板。
""",
        ROOT / "docs" / "信息论与编码" / "md" / "04-rate-distortion.md": """
# 信息论与编码 第 4 章摘要

## 课件定位

- 对应课件：`第4章 信息率失真函数.pdf`
- 主题：失真函数、平均失真、离散率失真函数、连续率失真函数

## 本讲主线

这章讨论有损压缩的理论下界：允许一定失真后，最少需要多少码率。

## 页序摘要

### 1. 失真度量

- 定义失真函数。
- 用失真矩阵表示离散信源的失真。
- 计算平均失真。

### 2. 离散信源率失真函数

- 由互信息最小化定义 `R(D)`。
- 分析 `R(D)` 的曲线和 `Dmax`。
- 讨论性质：单调不增、下凸。

### 3. 一般计算方法

- 课件给出参量表达式法。
- 通过给定概率分布与失真矩阵求 `R(D)`。

### 4. 连续信源率失真函数

- 讨论平方误差失真。
- 给出高斯信源的经典结果。

## 应掌握的结论

- `R(D)` 是最小互信息，不是最大互信息。
- `D=0` 回到无失真编码边界。
- `D` 增大时，理论最低码率下降。
""",
        ROOT / "docs" / "信息论与编码" / "md" / "05-source-coding.md": """
# 信息论与编码 第 5 章摘要

## 课件定位

- 对应课件：`第5章 信源编码.pdf`
- 主题：唯一可译码、即时码、平均码长、香农/费诺/哈夫曼/算术编码

## 本讲主线

这一章讨论如何构造尽量短、同时可正确译码的码。

## 页序摘要

### 1. 编码分类与基本要求

- 定长码与变长码。
- 唯一可译码、即时码和非即时码的关系。

### 2. 码树和判定条件

- 用码树表示前缀关系。
- 判断码字集合是否满足即时码要求。
- 计算平均码长和编码效率。

### 3. 香农编码、费诺编码、哈夫曼编码

- 香农码构造规则清晰。
- 费诺码依概率递归分组。
- 哈夫曼码给出最优前缀码。

### 4. 算术编码

- 把整个符号序列映射到一个概率区间。
- 压缩性能更接近理论极限。

### 5. 后续扩展

- 课件后部还涉及量化和常见编码方法简介。

## 应掌握的结论

- 构造最优变长前缀码优先想到哈夫曼编码。
- 即时码一定唯一可译，反过来不成立。
- 编码效率靠平均码长与熵的接近程度衡量。
""",
        ROOT / "docs" / "信息论与编码" / "md" / "06-channel-coding.md": """
# 信息论与编码 第 6 章摘要

## 课件定位

- 对应课件：`第6章 信道编码.pdf`
- 主题：检错纠错思想、线性分组码、汉明码、循环码、卷积码

## 本讲主线

第 6 章讨论如何通过增加监督位来提升传输可靠性。

## 页序摘要

### 1. 差错控制方式

- 反馈重发。
- 前向纠错。
- 混合纠错。

### 2. 线性分组码

- 用生成矩阵和校验矩阵描述码。
- 从向量空间角度构造许用码组。

### 3. 码距与纠错能力

- 最小码距决定检错能力和纠错能力。
- 给出典型定理和例题。

### 4. 汉明码

- 说明监督位个数关系。
- 通过校验矩阵和伴随式定位错误。

### 5. 循环码

- 介绍循环移位封闭性质。
- 用生成多项式构造系统循环码。
- 分析编码、校验和译码流程。

### 6. 卷积码与后续方案

- 介绍卷积码、状态图、网格图。
- 课件后部还引到 Turbo、TCM 等现代方案。

## 应掌握的结论

- `G/H`、最小码距、伴随式是分组码题的三件套。
- 汉明码是单错纠正的标准模型。
- 循环码常通过多项式方法处理。
""",
    }

    for path, content in files.items():
        write(path, content)


if __name__ == "__main__":
    main()
